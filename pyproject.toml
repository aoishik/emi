[project]
name = "emi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "vividsystem", email = "notrealbutnotobot@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "aiohttp>=3.14.3",
    "fastapi>=0.141.1",
    "pydantic>=2.13.5",
    "slack-bolt>=1.30.0",
    "starlette>=1.6.0",
    "uvicorn>=0.52.4",
]

[project.scripts]
emi = "emi:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
